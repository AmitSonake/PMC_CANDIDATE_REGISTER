<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.VoterSearch" parent="Base.Theme.VoterSearch">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="Theme.ButtonBackGround" parent="Base.Theme.VoterSearch">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:background">@drawable/candidate_gradient_shape</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="Theme.DrawableAlign" parent="Base.Theme.VoterSearch">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:drawableStart">@drawable/common_gradient</item>

    </style>

    <style name="Theme.CardButtonBackGround" parent="Base.Theme.VoterSearch">
        <!-- Transparent system bars for edge-to-edge. -->
        <item name="android:background">@drawable/card_btn_item</item>
        <item name="android:textSize">14sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>